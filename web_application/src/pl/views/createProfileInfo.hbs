<div class="jumbotron">
    <div class="container-sm">
        <h1 class="display-4 text-center">Personalize your profile!</h1>
        {{#if id}}
<form action="/profile/createInfo/{{id}}" method="POST">
  <div class="form-row">
    <div class="col-md-3 mb-3">
     <label>First name</label>
      <input type="text" name="firstname" class="form-control" >
    </div>
    <div class="col-md-3 mb-3">
          <label>Last name</label>
      <input type="text" name="lastname" class="form-control" >
     
    </div>
    <div class="col-md-3 mb-3">
          <label>Country</label>
      <input type="text" name="country" class="form-control" >
     
    
    </div>
      <div class="col-md-3 mb-3">
          <label>City</label>
      <input type="text" name="city" class="form-control" >
     
    
    </div>
  </div>
  <div class="form-row">
    <div class="col-md-6 mb-3">
        <label>Interest one</label>
    <select name="interest1" class="form-control" >
      {{#if interests.length}}
      {{#each interests}}
				
        <option value="{{interests_id}}">{{interest}}</option>
			{{/each}}
      
      {{/if}}

    </select>
    </div>
    <div class="col-md-6 mb-3">
          <label>Interest two</label>
      <select name="interest2" class="form-control" >
            {{#if interests.length}}
      {{#each interests}}
				
        <option value="{{interests_id}}">{{interest}}</option>
			{{/each}}
      
      {{/if}}
    </select>
     
    </div>


  </div>
   <div class="form-row">
        <div class="col-md-6 mb-3">
          <label>Interest three</label>
      <select name="interest3" class="form-control" >
            {{#if interests.length}}
      {{#each interests}}
				
        <option value="{{interests_id}}">{{interest}}</option>
			{{/each}}
      
      {{/if}}
    </select>
    </div>
    <div class="col-md-6 mb-3">
          <label>Interest four</label>
      <select name="interest4" class="form-control">
           {{#if interests.length}}
      {{#each interests}}
				
        <option value="{{interests_id}}">{{interest}}</option>
			{{/each}}
      
      {{/if}}
    </select>
     
    </div>
 

   </div>
  
  <!--<a class="text-decoration-none" href="home"><button type="submit" class="btn btn-success">Submit</button></a>-->
   <button type="submit" class="btn btn-success"  name="updateProfileInfoButton">Submit</button>
  {{!-- <a class="btn btn-success" href="home" role="button" type="submit" value="create">Submit</a> --}}
</form>
{{/if}}
</div>
</div>